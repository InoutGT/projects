# СРОЧНОЕ ИСПРАВЛЕНИЕ: Подключение к Supabase не работает

## 🔴 Проблема
DNS не может найти `db.zvcjvtszyksmbofmhkrt.supabase.co` - это означает, что проект приостановлен или недоступен.

## ✅ Решение 1: Проверить и запустить проект Supabase

1. **Откройте Supabase Dashboard:**
   - Перейдите на https://supabase.com/dashboard
   - Войдите в свой аккаунт

2. **Проверьте статус проекта:**
   - Найдите проект с ID `zvcjvtszyksmbofmhkrt`
   - Если проект показывает статус "Paused" или "Suspended" - нажмите **"Resume"** или **"Restore"**
   - Дождитесь полного запуска (2-3 минуты)

3. **Получите актуальную строку подключения:**
   - В проекте: **Settings** → **Database**
   - Найдите раздел **"Connection string"**
   - Выберите вкладку **"URI"** (для direct connection)
   - Скопируйте строку - она должна выглядеть так:
     ```
     postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres
     ```
     ИЛИ
     ```
     postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
     ```

4. **Обновите .env файл:**
   - Откройте файл `.env` в корне проекта
   - Замените `DATABASE_URL` на скопированную строку (замените `[YOUR-PASSWORD]` на реальный пароль)
   - Убедитесь, что пароль **без квадратных скобок**

## ✅ Решение 2: Создать новый проект Supabase (если старый не работает)

1. Зайдите на https://supabase.com/dashboard
2. Нажмите **"New Project"**
3. Заполните:
   - **Name:** task-manager (любое имя)
   - **Database Password:** придумайте пароль (запомните!)
   - **Region:** выберите ближайший
4. Нажмите **"Create new project"**
5. Дождитесь создания (2-3 минуты)
6. Скопируйте Connection String из **Settings → Database → Connection string → URI**
7. Обновите `.env` файл

## ✅ Решение 3: Использовать другой провайдер (быстрое решение)

### Вариант A: Neon (рекомендуется - проще всего)

1. Зайдите на https://neon.tech
2. Нажмите **"Sign Up"** (можно через GitHub)
3. Создайте новый проект
4. Скопируйте **Connection String** из дашборда
5. Обновите `.env`:
   ```env
   DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"
   DIRECT_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"
   ```

### Вариант B: Railway

1. Зайдите на https://railway.app
2. Нажмите **"Start a New Project"**
3. Выберите **"Database"** → **"PostgreSQL"**
4. Скопируйте **Connection URL** из настройков
5. Обновите `.env`

## После обновления .env

1. **Примените миграции:**
   ```bash
   npx prisma migrate deploy
   ```

2. **Проверьте подключение:**
   ```bash
   npx prisma db pull
   ```

3. **Запустите сервер:**
   ```bash
   npm run dev
   ```

## Проверка правильности строки подключения

Строка должна:
- ✅ Начинаться с `postgresql://` или `postgres://`
- ✅ Содержать пароль **без квадратных скобок** `[]`
- ✅ Иметь правильный формат: `postgresql://user:password@host:port/database`
- ✅ Для Supabase: использовать либо `db.xxx.supabase.co:5432` (direct) либо `aws-0-xxx.pooler.supabase.com:6543` (pooling)

## Если ничего не помогает

Попробуйте использовать локальную базу данных для тестирования:

1. Установите PostgreSQL локально
2. Создайте базу: `createdb task_manager`
3. Обновите `.env`:
   ```env
   DATABASE_URL="postgresql://postgres:postgres@localhost:5432/task_manager?schema=public"
   ```
