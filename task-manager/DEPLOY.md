# 🚀 Деплой на Vercel - Пошаговая инструкция

Это пошаговая инструкция для деплоя Task Manager на Vercel (самый простой вариант).

## 📋 Подготовка

### Шаг 1: Создайте аккаунт на Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Зарегистрируйтесь через GitHub (рекомендуется) или email
3. Подтвердите email (если регистрация через email)

### Шаг 2: Подготовьте репозиторий GitHub

1. Создайте новый репозиторий на GitHub:
   - Перейдите на [github.com/new](https://github.com/new)
   - Название: `task-manager` (или любое другое)
   - Выберите **Public** или **Private**
   - **Не** добавляйте README, .gitignore или лицензию (они уже есть)

2. Инициализируйте Git в вашем проекте (если еще не сделано):
   ```bash
   cd D:\projects\task-manager
   git init
   git add .
   git commit -m "Initial commit"
   ```

3. Подключите к GitHub репозиторию:
   ```bash
   git remote add origin https://github.com/ваш-username/task-manager.git
   git branch -M main
   git push -u origin main
   ```

## 🗄️ Шаг 3: Настройте базу данных PostgreSQL

Vercel предоставляет встроенный PostgreSQL через Vercel Postgres.

### Вариант А: Vercel Postgres (рекомендуется - проще)

**Важно:** Создайте БД **после** первого деплоя проекта на Vercel, чтобы переменные окружения автоматически подключились.

1. В дашборде Vercel после создания проекта:
   - Перейдите на вкладку **Storage**
   - Нажмите **Create Database**
   - Выберите **Postgres**
   - Выберите план (Hobby - бесплатный для начала)
   - Выберите регион (ближайший к вам)
   - Нажмите **Create**

2. После создания БД:
   - Vercel автоматически создаст переменные `POSTGRES_PRISMA_URL`, `POSTGRES_URL` и `POSTGRES_URL_NON_POOLING`
   - Они автоматически добавятся в Environment Variables проекта
   - **Используйте `POSTGRES_PRISMA_URL` как `DATABASE_URL`** (обновите `prisma/schema.prisma` или используйте переменную напрямую)

### Вариант Б: Внешняя база данных

Можно использовать:
- **Supabase** (бесплатный tier) - [supabase.com](https://supabase.com)
- **Neon** (бесплатный tier) - [neon.tech](https://neon.tech)
- **Railway** (бесплатный tier) - [railway.app](https://railway.app)

После создания БД скопируйте connection string (он будет в формате `postgresql://...`)

## 🌐 Шаг 4: Деплой на Vercel

### Вариант 1: Через веб-интерфейс (проще для начинающих)

1. **Импортируйте проект:**
   - Перейдите на [vercel.com/new](https://vercel.com/new)
   - Нажмите **Import Git Repository**
   - Выберите ваш репозиторий `task-manager`
   - Нажмите **Import**

2. **Настройте проект:**
   - **Framework Preset:** Next.js (определится автоматически)
   - **Root Directory:** `./` (оставьте по умолчанию)
   - **Build Command:** `npm run build` (оставьте по умолчанию)
   - **Output Directory:** `.next` (оставьте по умолчанию)
   - **Install Command:** `npm install` (оставьте по умолчанию)

3. **Добавьте переменные окружения:**
   
   Нажмите **Environment Variables** и добавьте:
   
   | Ключ | Значение | Описание |
   |------|----------|----------|
   | `DATABASE_URL` | `postgresql://...` | Connection string к БД (если используете внешнюю БД) |
   | `POSTGRES_PRISMA_URL` | Автоматически | Если используете Vercel Postgres, создается автоматически |
   | `POSTGRES_URL_NON_POOLING` | Автоматически | Для Prisma Migrate, создается автоматически |
   | `NEXTAUTH_SECRET` | `ваш-секретный-ключ` | Сгенерируйте командой: `openssl rand -base64 32` |
   | `NEXTAUTH_URL` | `https://ваш-домен.vercel.app` | URL вашего деплоя (можно изменить после деплоя) |

   **Важно:** 
   - Если используете Vercel Postgres, `DATABASE_URL` не нужна - используйте `POSTGRES_PRISMA_URL`
   - Для `NEXTAUTH_URL` сначала укажите временный URL, потом обновите после первого деплоя

4. **Деплой:**
   - Нажмите **Deploy**
   - Дождитесь завершения сборки (обычно 2-3 минуты)
   - Если сборка успешна - проект задеплоен! 🎉

### Вариант 2: Через Vercel CLI (для продвинутых)

1. **Установите Vercel CLI:**
   ```bash
   npm i -g vercel
   ```

2. **Войдите в аккаунт:**
   ```bash
   vercel login
   ```

3. **Деплой:**
   ```bash
   cd D:\projects\task-manager
   vercel
   ```
   
   Следуйте инструкциям:
   - Link to existing project? **No** (для первого деплоя)
   - What's your project's name? **task-manager**
   - In which directory is your code located? **./**
   - Want to override settings? **No**

4. **Добавьте переменные окружения:**
   ```bash
   vercel env add DATABASE_URL
   vercel env add NEXTAUTH_SECRET
   vercel env add NEXTAUTH_URL
   ```

5. **Продакшн деплой:**
   ```bash
   vercel --prod
   ```

## 🔧 Шаг 5: Настройка базы данных после деплоя

После первого деплоя нужно применить миграции Prisma.

### Если используете Vercel Postgres:

1. **Установите Prisma Migrate локально:**
   ```bash
   npm install -g prisma
   ```

2. **Получите connection string:**
   - В дашборде Vercel → ваш проект → Storage → ваша БД
   - Скопируйте `POSTGRES_URL_NON_POOLING` (не путать с `POSTGRES_PRISMA_URL`)

3. **Примените миграции:**
   ```bash
   cd D:\projects\task-manager
   $env:DATABASE_URL="postgresql://..."; npx prisma migrate deploy
   ```
   
   Или в PowerShell:
   ```powershell
   $env:DATABASE_URL="ваш-postgres-url-non-pooling"
   npx prisma migrate deploy
   ```

### Если используете внешнюю БД:

1. Создайте файл `.env` локально с вашим `DATABASE_URL`
2. Запустите миграции:
   ```bash
   npx prisma migrate deploy
   ```

## 🔐 Шаг 6: Обновите NEXTAUTH_URL

После деплоя обновите переменную `NEXTAUTH_URL`:

1. В дашборде Vercel → ваш проект → Settings → Environment Variables
2. Найдите `NEXTAUTH_URL`
3. Измените на реальный URL: `https://ваш-проект.vercel.app`
4. Передеплойте проект (Redeploy)

## ✅ Шаг 7: Проверка

1. Откройте ваш проект: `https://ваш-проект.vercel.app`
2. Зарегистрируйтесь
3. Проверьте, что все работает:
   - ✅ Регистрация и вход
   - ✅ Создание проектов
   - ✅ Создание задач
   - ✅ Drag-and-drop на Kanban доске
   - ✅ Аналитика

## 📝 Важные замечания

### Переменные окружения для Production:

Убедитесь, что все переменные добавлены для **Production**, **Preview** и **Development** окружений:

- `DATABASE_URL` (или `POSTGRES_PRISMA_URL` для Vercel Postgres)
- `NEXTAUTH_SECRET` (должен быть минимум 32 символа)
- `NEXTAUTH_URL` (ваш production URL)

### Генерация NEXTAUTH_SECRET:

На Windows (PowerShell):
```powershell
[Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32))
```

Или онлайн: [generate-secret.vercel.app/32](https://generate-secret.vercel.app/32)

### Troubleshooting

**Ошибка при сборке:**
- Убедитесь, что `postinstall` скрипт добавлен в `package.json`
- Проверьте, что Prisma Client генерируется правильно

**Ошибка подключения к БД:**
- Проверьте переменные окружения
- Убедитесь, что миграции применены
- Для Vercel Postgres используйте `POSTGRES_PRISMA_URL`, а не `DATABASE_URL`

**Ошибки NextAuth:**
- Проверьте `NEXTAUTH_SECRET` (должен быть установлен)
- Проверьте `NEXTAUTH_URL` (должен совпадать с доменом проекта)

## 🎉 Готово!

После успешного деплоя ваш Task Manager будет доступен по адресу:
`https://ваш-проект.vercel.app`

Vercel автоматически деплоит каждое обновление в GitHub репозитории!

---

**Нужна помощь?** 
- Документация Vercel: [vercel.com/docs](https://vercel.com/docs)
- Документация Prisma: [prisma.io/docs](https://www.prisma.io/docs)
