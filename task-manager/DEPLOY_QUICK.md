# ⚡ Быстрый деплой на Vercel

Краткая версия инструкции для тех, кто уже знаком с Vercel.

## 🚀 Шаги деплоя

### 1. GitHub репозиторий
```bash
cd D:\projects\task-manager
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/task-manager.git
git branch -M main
git push -u origin main
```

### 2. Деплой на Vercel

1. Зайдите на [vercel.com/new](https://vercel.com/new)
2. Импортируйте ваш GitHub репозиторий
3. **Не деплойте сразу!** Сначала настройте переменные окружения:

### 3. База данных (Vercel Postgres)

1. После импорта проекта → вкладка **Storage** → **Create Database** → **Postgres**
2. Выберите Hobby план (бесплатный)
3. Vercel автоматически добавит переменные:
   - `POSTGRES_PRISMA_URL`
   - `POSTGRES_URL_NON_POOLING`

### 4. Переменные окружения

В Settings → Environment Variables добавьте:

| Переменная | Значение | Откуда взять |
|------------|----------|--------------|
| `NEXTAUTH_SECRET` | Сгенерируйте | `openssl rand -base64 32` или онлайн генератор |
| `NEXTAUTH_URL` | `https://ваш-проект.vercel.app` | Обновите после первого деплоя |

**Примечание:** `POSTGRES_PRISMA_URL` добавляется автоматически при создании БД.

### 5. Первый деплой

Нажмите **Deploy** и дождитесь завершения.

### 6. Применение миграций

После успешного деплоя, локально:

```powershell
# Получите POSTGRES_URL_NON_POOLING из Vercel дашборда
$env:DATABASE_URL="ваш-postgres-url-non-pooling"
npx prisma migrate deploy
```

### 7. Обновите NEXTAUTH_URL

После первого деплоя обновите `NEXTAUTH_URL` на реальный URL и передеплойте.

## ✅ Готово!

Проект доступен по адресу: `https://ваш-проект.vercel.app`

---

**Полная инструкция:** см. [DEPLOY.md](./DEPLOY.md)
