# 📋 Task Manager MVP

Современный task-менеджер в стиле Trello/Linear с полнофункциональной системой управления проектами, задачами и командной работой.

![Next.js](https://img.shields.io/badge/Next.js-16.1.1-black?style=flat&logo=next.js)
![TypeScript](https://img.shields.io/badge/TypeScript-5.0-blue?style=flat&logo=typescript)
![Prisma](https://img.shields.io/badge/Prisma-5.18-2D3748?style=flat&logo=prisma)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-Latest-336791?style=flat&logo=postgresql)
![Tailwind CSS](https://img.shields.io/badge/Tailwind-4.0-38B2AC?style=flat&logo=tailwind-css)

## ✨ Функционал

### 🔐 Аутентификация и авторизация
- Регистрация с email и паролем
- Вход в систему (NextAuth v5)
- Защита роутов через middleware
- Сессии хранятся в базе данных

### 👥 Проекты и команды
- Создание проектов
- Управление участниками проектов (добавление/удаление)
- Список всех проектов пользователя
- Детальный просмотр проекта с информацией об участниках

### 📊 Задачи и Kanban-доска
- Создание задач с полями:
  - Название и описание
  - Приоритет (LOW, MEDIUM, HIGH)
  - Статус (To Do, In Progress, Review, Done)
  - Дедлайн
  - Назначенный исполнитель
- **Drag-and-drop** перетаскивание задач между колонками (с использованием @dnd-kit)
- Kanban-доска с 4 статусами
- Визуальные индикаторы приоритетов
- Автоматическое отслеживание просроченных задач

### 📈 Аналитика
- Количество задач по статусам
- Общая статистика по задачам
- Количество просроченных задач
- Визуальный график распределения задач
- Карточки статистики в минималистичном дизайне

### 🎨 UI/UX
- Минималистичный дизайн в стиле Linear
- Темная цветовая схема
- Адаптивный дизайн для всех устройств
- Плавные анимации и переходы
- Интуитивно понятный интерфейс

## 🛠 Технологический стек

### Frontend
- **Next.js 16** (App Router) - React-фреймворк с серверными компонентами
- **TypeScript** - типобезопасность
- **Tailwind CSS 4** - утилитарный CSS-фреймворк
- **@dnd-kit** - библиотека для drag-and-drop функциональности
- **date-fns** - работа с датами

### Backend
- **Next.js API Routes** - серверные API endpoints
- **Server Actions** - серверные действия Next.js
- **NextAuth v5** (beta) - аутентификация и авторизация
- **Prisma 5** - ORM для работы с базой данных
- **PostgreSQL** - реляционная база данных
- **bcrypt** - хеширование паролей
- **Zod** - валидация данных

### Инфраструктура
- **Vercel** - платформа для деплоя (рекомендуется)

## 🚀 Локальный запуск

### Предварительные требования

- **Node.js** 20.x или выше
- **PostgreSQL** 14+ (локально или удаленно)
- **npm** или **yarn**

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/ваш-username/task-manager.git
cd task-manager
```

### Шаг 2: Установка зависимостей

```bash
npm install
```

### Шаг 3: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# База данных PostgreSQL
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/task_manager?schema=public"

# NextAuth секретный ключ (сгенерируйте случайную строку минимум 32 символа)
NEXTAUTH_SECRET="ваш-секретный-ключ-минимум-32-символа"

# URL приложения (для локальной разработки)
NEXTAUTH_URL="http://localhost:3000"
```

**Генерация NEXTAUTH_SECRET:**

Windows (PowerShell):
```powershell
[Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32))
```

macOS/Linux:
```bash
openssl rand -base64 32
```

Онлайн генератор: [generate-secret.vercel.app/32](https://generate-secret.vercel.app/32)

### Шаг 4: Настройка базы данных

1. **Создайте базу данных PostgreSQL:**

   ```bash
   createdb task_manager
   ```

   Или через psql:
   ```bash
   psql -U postgres
   CREATE DATABASE task_manager;
   \q
   ```

2. **Сгенерируйте Prisma Client:**

   ```bash
   npx prisma generate
   ```

3. **Примените миграции:**

   ```bash
   npx prisma migrate dev --name init
   ```

   Это создаст все необходимые таблицы в базе данных.

4. **(Опционально) Откройте Prisma Studio для просмотра данных:**

   ```bash
   npx prisma studio
   ```

   Откроется веб-интерфейс по адресу `http://localhost:5555` для просмотра и редактирования данных.

### Шаг 5: Запуск сервера разработки

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### Шаг 6: Первый вход

1. Перейдите на http://localhost:3000
2. Вас автоматически перенаправит на страницу входа
3. Нажмите "Зарегистрируйтесь"
4. Заполните форму регистрации:
   - Имя пользователя
   - Email
   - Пароль (минимум 8 символов)
5. После регистрации вы автоматически войдете в систему
6. Будет создан ваш личный workspace с дефолтным бордом

## 📁 Структура проекта

```
task-manager/
├── prisma/
│   └── schema.prisma          # Схема базы данных
├── public/                    # Статические файлы
├── src/
│   ├── app/                   # Next.js App Router
│   │   ├── (dashboard)/       # Защищенные страницы
│   │   │   ├── page.tsx       # Главная страница (доски)
│   │   │   ├── projects/      # Проекты
│   │   │   └── tasks/         # Задачи и Kanban
│   │   ├── (public)/          # Публичные страницы
│   │   │   ├── signin/        # Вход
│   │   │   └── signup/        # Регистрация
│   │   └── api/               # API endpoints
│   │       ├── auth/          # NextAuth routes
│   │       └── register/      # Регистрация
│   ├── components/            # React компоненты
│   │   ├── analytics/         # Компоненты аналитики
│   │   ├── kanban/            # Kanban доска
│   │   └── ui/                # UI компоненты
│   ├── lib/                   # Утилиты и конфигурация
│   │   ├── prisma.ts          # Prisma Client
│   │   ├── validators.ts      # Zod схемы
│   │   └── utils.ts           # Утилиты
│   ├── auth.config.ts         # Конфигурация NextAuth
│   ├── auth.ts                # NextAuth экспорты
│   └── middleware.ts          # Защита роутов
├── .env.example               # Пример переменных окружения
├── .gitignore                 # Git ignore файлы
├── DEPLOY.md                  # Инструкция по деплою
├── next.config.ts             # Конфигурация Next.js
├── package.json               # Зависимости и скрипты
└── tsconfig.json              # Конфигурация TypeScript
```

## 🎯 Основные возможности

### Управление проектами
- ✅ Создание проектов с описанием
- ✅ Приглашение участников по email
- ✅ Управление участниками (добавление/удаление)
- ✅ Просмотр списка всех проектов

### Управление задачами
- ✅ Создание задач с полной информацией
- ✅ Drag-and-drop между статусами
- ✅ Приоритеты и дедлайны
- ✅ Назначение исполнителей
- ✅ Отслеживание просроченных задач

### Визуализация и аналитика
- ✅ Kanban-доска с 4 колонками
- ✅ Статистика по статусам
- ✅ График распределения задач
- ✅ Отслеживание просроченных задач

## 🔧 Доступные команды

```bash
# Разработка
npm run dev          # Запуск dev-сервера (http://localhost:3000)

# Сборка
npm run build        # Production сборка
npm run start        # Запуск production сервера

# База данных
npx prisma generate  # Генерация Prisma Client
npx prisma migrate dev --name <name>  # Создание новой миграции
npx prisma migrate deploy             # Применение миграций в production
npx prisma studio    # Открыть Prisma Studio

# Код
npm run lint         # Проверка кода через ESLint
```

## 🌐 Деплой

Проект готов к деплою на Vercel. Подробные инструкции см. в файле [DEPLOY.md](./DEPLOY.md).

**Быстрый старт:**
1. Загрузите код на GitHub
2. Импортируйте проект на [vercel.com](https://vercel.com)
3. Создайте Vercel Postgres базу данных
4. Настройте переменные окружения
5. Деплойте!

## 📝 Переменные окружения

| Переменная | Описание | Пример |
|------------|----------|--------|
| `DATABASE_URL` | Connection string к PostgreSQL | `postgresql://user:pass@host:5432/db` |
| `POSTGRES_PRISMA_URL` | Prisma connection string (для Vercel Postgres) | Автоматически создается Vercel |
| `NEXTAUTH_SECRET` | Секретный ключ для NextAuth (32+ символов) | Генерируется случайно |
| `NEXTAUTH_URL` | URL вашего приложения | `http://localhost:3000` или `https://your-app.vercel.app` |

## 🐛 Решение проблем

### Ошибка подключения к базе данных
- Проверьте, что PostgreSQL запущен
- Убедитесь, что `DATABASE_URL` корректна
- Проверьте права доступа к базе данных

### Ошибки Prisma
- Убедитесь, что миграции применены: `npx prisma migrate deploy`
- Сгенерируйте клиент: `npx prisma generate`

### Ошибки NextAuth
- Проверьте, что `NEXTAUTH_SECRET` установлен
- Убедитесь, что `NEXTAUTH_URL` соответствует домену

### Ошибки сборки
- Удалите `.next` и `node_modules`, затем переустановите:
  ```bash
  rm -rf .next node_modules
  npm install
  npm run build
  ```

## 📚 Документация

- [Next.js Documentation](https://nextjs.org/docs)
- [Prisma Documentation](https://www.prisma.io/docs)
- [NextAuth.js Documentation](https://next-auth.js.org)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [@dnd-kit Documentation](https://docs.dndkit.com)

## 📄 Лицензия

MIT

## 🤝 Вклад

Это MVP проект. Если хотите внести свой вклад:
1. Fork репозиторий
2. Создайте feature branch
3. Commit изменения
4. Push в branch
5. Создайте Pull Request

---

**Создано с ❤️ используя Next.js, TypeScript и современные веб-технологии**
